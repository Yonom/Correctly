steps:
# Get ENCRYPTION_KEY
- name: gcr.io/cloud-builders/gcloud
  entrypoint: 'bash'
  args: [ '-c','gcloud secrets versions access latest --secret=ENCRYPTION_KEY > ENCRYPTION_KEY' ]
# Build the container image
- name: 'gcr.io/cloud-builders/docker'
  entrypoint: 'bash'
  args: [ '-c', 'docker build -t gcr.io/$PROJECT_ID/server:latest --cache-from gcr.io/$PROJECT_ID/server:latest --build-arg ENCRYPTION_KEY=$(<ENCRYPTION_KEY) .' ]
# Push the container image to Container Registry
- name: 'gcr.io/cloud-builders/docker'
  args: ['push', 'gcr.io/$PROJECT_ID/server:latest']
# Deploy container image to Cloud Run
- name: gcr.io/cloud-builders/gcloud
  args: [
    'run', 'deploy', 
    'server', 
    '--image', 'gcr.io/$PROJECT_ID/server:latest', 
    '--region', 'europe-west4', 
    '--platform', 'managed',
    '--allow-unauthenticated'
  ]
images: ['gcr.io/$PROJECT_ID/server:latest']
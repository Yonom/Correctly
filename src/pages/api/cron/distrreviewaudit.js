// Laden HW, REV, Variante mit params, USERS

// Handler welche Methode (A/B(B/C))
// Verteilungen

// Review Audit erstellen

const test = async (req, res) => {
  /**
   *
   */
  function shuffle() {
    // random mischen Array

  }

  /**
   *
   */
  function treshhold() {
    // Identifizieren der Abweichung + Markieren bei zu hoch

  }

  // Handler
  // Variante Laden
  if (variante == 'A') {
    // Stichprobengröße
    // Randomly Stichprobengröße aus Solutions ziehen
    // Zugehörigen Review laden
    // In Liste Review Audits
  } else if (variante == 'B') {
    // alpha hören
    for (i = 0; i < SolutionsList; i++) {
      // Load reviews for Solution
      if (nbrreview < 2) {
        // Keine 2 Reviews abgegeben
        // Wer Reviewer nicht abgegeben?
        // Reviewer auf 0 Punkte
        // to the ToDo_List of the lecturer/Module_Coordinator and assign them the attribute currently_in_audit;
      }
      // Treshhold Prüfen
      if (treshhold > alpha) {
        // In Liste ReviewAudit
      }
    }
  } else {
    // Fehler
  }

  // Liste ReviewAudits zu Reviewaudits
};

export default test;
